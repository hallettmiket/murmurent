"""Wigamig Claude Code hooks. Each module is independently invokable as a
script (`python -m wigamig.hooks.<name>`) so the CC harness can register them
in `~/.claude/settings.json` without importing the rest of the wigamig package.
"""
