"""Murmurent Claude Code hooks. Each module is independently invokable as a
script (`python -m murmurent.hooks.<name>`) so the CC harness can register them
in `~/.claude/settings.json` without importing the rest of the murmurent package.
"""
